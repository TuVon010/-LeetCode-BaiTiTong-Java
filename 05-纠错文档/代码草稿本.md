
```java
// 1. 把字符串 "hello" 转成字符数组
String s = "hello";
char[] arr = __s.toCharArray()____;    // ← 填什么？

// 2. 判断两个字符串内容是否相等
String a = "hi", b = "hi";
boolean same = _a.equals(b)_____;   // ← 填什么？（不能用 ==）

// 3. 取字符串 "world" 的第 3 个字符
char ch = "world".__charAt____(3);   // ← 填什么方法？

// 4. 截取 "abcdef" 的第 2~4 个字符（不含第4）→ "bcd"
String sub = "abcdef"._substring_(1, 4);   // ← 填什么方法？

// 5. "Java" 转大写
String upper = "Java"._toUpperCase()_____;   // ← 填什么？
```
**Q4 巩固：** 默写移动零的核心代码（任选交换版或覆盖版）：

```java
public void moveZeroes(int[] nums) {

    for(int fast=0,slow=0;fast<nums.length;fast++){
	    if(nums[fast]!=0){
	    temp=nums[slow];
	    nums[slow]=nums[fast];
	    nums[fast]=temp;
	    slow++;
	    }
    }
}
```

java

**Q7 巩固：** 无重复子串的收缩条件是？用 Java 写那 2 行 while：

```java
while (set.contains(charAt(i))) {
    set.remove(charAt(left));
    left++;
}

```

java

**Q8 巩固：** 单调队列新元素入队前，那一行 while：
```java
while (!deque.isEmpty()&&deque.peekLast()<nums[i]) {
    deque.removeLast();
}
deque.addLast(nums[i]);
```

java

---

## 56. 合并区间 · 带注释学习版（2026-08-03）

```java
import java.util.*;

class Solution {
    public int[][] merge(int[][] intervals) {
        // ========== 知识点①：Lambda 表达式排序 ==========
        // 二维数组默认不知道按什么排，需要告诉它规则
        // (a, b) -> 是"匿名函数"：a 和 b 是相邻两个区间
        // Integer.compare(a[0], b[0]) 比较两个区间的起点
        //   返回负数 → a 排前（起点小）；正数 → b 排前；0 → 相等
        // 效果：按起点从小到大升序排列
        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));

        // ========== 知识点②：为什么用 List<int[]> ==========
        // 一个区间 = [start, end] 两个数，正好用一个 int[] 装
        // List<int[]> = 每个元素是一个区间数组
        // List<Integer> 只能装一个数，装不下"区间"概念
        List<int[]> merge = new ArrayList<>();

        // ========== 知识点③：遍历二维数组 ==========
        // intervals 是 int[][], for-each 遍历出的每个元素是 int[]
        // interval[0] = 起点, interval[1] = 终点
        for (int[] interval : intervals) {

            // ========== 知识点④：判断重叠 ==========
            // merge.isEmpty() 空列表，直接加第一个区间
            // interval[0] > merge.get(merge.size()-1)[1]
            //   merge.get(merge.size()-1) = 取列表最后一个区间
            //   .size() 是 List 的长度方法（数组用 .length 无括号，String 用 .length()）
            //   [1] 是那个区间的终点
            // 条件翻译：当前区间起点 > 最后一个区间终点 → 不重叠
            // 注意用 > 不是 >= ：端点相接（==）算重叠，要合并
            if (merge.isEmpty() || interval[0] > merge.get(merge.size() - 1)[1]) {
                // 不重叠：新建一个区间副本加入
                // new int[]{interval[0], interval[1]} 新建副本，不存原数组引用（避免污染输入）
                merge.add(new int[]{interval[0], interval[1]});
            } else {
                // ========== 知识点⑤：引用类型（最重要） ==========
                // int[] last = merge.get(...) 不是拷贝！
                // 它只是"指向" merge 里那个数组对象的引用（相当于给钥匙）
                // 所以改 last[1]，merge 里的那个数组也会变 —— 这正是我们想要的合并效果
                int[] last = merge.get(merge.size() - 1);
                // 合并：终点取较大值（可能新区间被完全包含在内）
                last[1] = Math.max(last[1], interval[1]);
            }
        }

        // ========== 知识点⑥：toArray 转换 ==========
        // merge 是 List，题目要 int[][]
        // list.toArray(参数) 需要传一个数组来确定返回类型
        // new int[merge.size()][]  = "买一个 size 层的空书架"
        //    只写外层长度 size()，不写内层长度
        //    因为 List 里的 int[] 元素已经现成了，toArray 会自己放进去
        //    （内层长度是创建完整二维数组时才写，如 new int[3][2]）
        // 也可以写 new int[0][] —— toArray 发现不够大会自动扩容
        return merge.toArray(new int[merge.size()][]);
    }
}
```

**验证用例：**
- `[[1,3],[2,6],[8,10],[15,18]]` → `[[1,6],[8,10],[15,18]]`
- `[[1,4],[4,5]]` → `[[1,5]]`（端点相接算重叠）
-
## 🔄 到期复习 · 小测

### REV-09：charAt vs toCharArray

**Q1：** 补全代码，从字符串 `s = "hello"` 取出第 2 个字符（应该是 `'e'`）：

```java
char ch = s.charAt(1);   // ← 填什么？
```

java

**Q2：** 把字符串 `s = "hello"` 变成字符数组 `{'h','e','l','l','o'}`：

```java
char[] arr = s._toCharArray();   // ← 填什么？
```

java

**Q3：** 判断对错——`"abc".toCharArray()[1]` 的结果是 `'b'`。（对还是错？应该是对的吧，意思是先变成字符数组，然后找下标为1的值）

---

### REV-04：Lambda + toArray

**Q4：** 把二维数组 `intervals` 按起点（`[0]`）升序排序，填出 Lambda：

```java
Arrays.sort(intervals,(a,b)->(a[0]-b[0]));
//或者，我也有点忘了感觉
Arrays.sort(intervals,(a,b)->Integer.compare(a[0]-b[0]));
   // ← 填什么？
```

java

**Q5：** 把 `List<int[]> merge` 转成 `int[][]`，填出 toArray：

```java
int[][] result = merge.______;   // ← 填什么？不知道
```

java

**Q6：** `List` 的长度方法是什么？数组呢？String 呢？（三个都写出来）

```java
merge.size()   // List 长度
nums.length     // 数组长度
s.length()       // String 长度
```


20260806
##  复习小测 · 二维数组（3 分钟）

拿出草稿纸，凭记忆答（不许翻文档）：

```java
int[][] matrix = new int[3][5];   // 3行5列
```

java

**Q1（填空）：**

- 行数 = `matrix.length
- 列数 = `matrix[0].length

**Q2（填空）：** 第 2 行第 4 列的元素 → `matrix[1][39]`

**Q3（写代码）：** 把**第 0 行**整行置为 0（用双 for 循环的框架）：

```java
for (int j = 0; j < _matrix[0].length; j++) {
    matrix[0][j] = 0;
}
```


**Q4（写代码）：** 把**第 1 列**整列置为 0：

```java
for (int i = 0; i < matrix.length______; i++) {
    matrix[i][_0] = 0;
}
```

java

**Q5（陷阱题·判断对错）：** 下面代码想"把整列置零"，对吗？

```java
for (int j = 0; j < n; j++) {
    matrix[1][j] = 0;    // 这改的是第几列？这是第一行
}
```

java
### 附加小测（3 题，来自到期的复习项）

**B1（REV-04 · Lambda + toArray）** 补全 56 题的两行代码：

```java
Arrays.sort(intervals, (a, b) -> a[0]-b[0]或者 Integer.compare(a[0],b[0])______);   // 按区间起点升序排序
return res.toArray(new _int[res.size()][0]);          // List<int[]> → int[][]
```

java

**B2（REV-05 · String 复习卡）** 判断对错，说明理由：

```java
String s = "abc";
int len = s.length;      // ① 这行对吗？错
int len2 = s.length();   // ② 这行对吗？对
```

java

**B3（REV-08 · 覆盖语义）** 判断"窗口是否涵盖 t"，应该用哪个？为什么？

```java
// A: if (cntS[i] != cntT[i]) return false;   // 完全相等才算涵盖
// B: if (cntS[i] < cntT[i]) return false;    // 有一个不够就不涵盖这个吧，为什么我感觉表达不出来
```

java

## Part A · REV-04/05/06/08 检验（8 题）

**REV-04（Lambda + toArray）**

**A1（判断）：** `Arrays.sort(intervals, (a, b) -> a[0] - b[0])` 能按区间起点升序排序。 对 

**A2（判断）：** `res.toArray(new int[0][])` 能把 `List<int[]>` 转成 `int[][]`。  错,应该是`res.toArray(new int[res.size][])`

**REV-05（String + Deque）**

**A3（判断）：** 字符串长度用 `s.length`，数组长度用 `arr.length()`。 错,应该是s.length(),arr.length

**A4（判断）：** 用 `Deque` 当队列：`addLast` 入队、`pollFirst` 出队。 对 ,出队也可以用removeFirst(),查看队首是peekFirst()

**REV-06（前缀和 + 哈希）**

**A5（判断）：** 560 题的核心是"子数组和 = 前缀和之差"，即 `sum[i] - sum[j] == k`。 对 

**A6（填空）：** 边遍历边查前缀和，查到 `map.get(sum - k)` 就累加答案。请问：应该**先查再存**，还是**先存再查**？（一句话）为什么？先查再存，如果先存再查的话最简单的例子是和为0，sum[0]=0,这个就会把本身算上吧，我感觉我描述的不太清楚

**REV-08（覆盖语义 + 下标）**

**A7（判断）：** "窗口涵盖 t" = 窗口里每个字符数量**恰好等于** t 的需求。 错，不小于就行了

**A8（判断）：** 计数数组用字符 ASCII 当下标（如 `cntS[s.charAt(i)]`），而窗口位置用 left/right 变量存。 对 ？

---

## Part B · ERR-006~014 判断（10 题）

**B1（ERR-006）：** 滑动窗口收缩时用 `if` 就够了，`while` 是多余的。 错，不满足条件就一直缩，应该是这样吧，我感觉我说的也有问题

**B2（ERR-006）：** 窗口长度（闭区间）公式是 `right - left`。 错，应该是`right - left+1`，模拟一下就知道了

**B3（ERR-007）：** `char c = s.atChar(1)` 能从字符串取第 1 个字符。 错，应该是能取到下标为1的吧，下标为1应该是第二个，这个我有点不熟悉不知道我说的对不对

**B4（ERR-008）：** 单调队列里，新元素比队尾大时，用 `if` 弹出一次就够。 错，新元素更大，那他前面所有比他小的都不可能成为新窗口的最大值，所以要用while全部弹出

**B5（ERR-009）：** 从窗口删字符写成 `cntS[left++]--` 是正确的。 对 的，相当于字符计数次数先减1再增加left来缩小窗口

**B6（ERR-010）：** 判断"某字符不够"用 `cntS[i] != cntT[i]`。  错，应该不小于就行了

**B7（ERR-011）：** `k=15, n=6` 时，可以先写 `k %= n`，然后 `k` 变成几？3

**B8（ERR-012）：** 两遍扫描的第二遍应该用 `=`（覆盖）而不是 `*=`（叠加）。 错，第一次算的左边的，第二次是再左边的基础上乘右边的，所以要用叠加

**B9（ERR-013）：** 原地哈希交换归位时，条件里必须有 `nums[nums[i]-1] != nums[i]`。 对 的吧，这我也有点迷糊

**B10（ERR-014）：** 螺旋矩阵走"下边"前要检查 `top <= bottom`。 对 的，但是我对这个理解也是比较迷糊

## ✋ 自测 3 题（防止"以为自己会了"）

**Q1（概念）：** 链表节点 `head`，怎么判断链表是空的？head=null;

**Q2（遍历）：** 数一数链表有几个节点（写循环）：

```java
int count = 0;
ListNode cur = head;
while (cur_) {
    count++;
    cur = cur.next______;
}
```

**Q3（理解）：** 为什么链表的"随机访问"很慢？数组为什么快？
我感觉因为链表只能从链上一个遍历，数组有下标，可以直接访问，感觉这一块我也不太明白

20260807
**R1（REV-14 · 递归反转）：** 补全递归反转的核心两行：

```java
ListNode newHead = reverseList(head.next);
head.next.next=head______;   // 把 head 接到子链尾巴上（head.next 是尾巴）
_head.next=null__;   // head 变成新尾巴,虽然能想明白，还是比较绕
```

java

**R2（REV-15 · 快慢指针）：** 141 判环的 while 条件是什么？（写完整）快慢指针相遇

**R3（REV-15 · 142 找入口）：** 快慢相遇后，怎么找环入口？一句话说思路。
将快指针重新指向链表头，然后速度保持和慢指针一样，然后这时候快慢指针同时走，相遇的点即为入口

**R4（ERR-009 盯防 · 陷阱）：** 下面哪行是对的？第二行

```java
// A: cntS[left++]--          // 从窗口删字符
// B: cntS[S.charAt(left++)]--
```

java

---

答完这 4 题，马上开始 **21 合并两个有序链表**（Easy，练"虚拟头节点"套路，你会觉得轻松）💪

```java
class Solution {
    public ListNode reverseKGroup(ListNode head, int k) {
        ListNode dummy = new ListNode(-1);
        ListNode pre=dummy;
        dummy.next=head;
        ListNode cur=head;
        
        while(true){
        //首先判断是否能凑成一组
		    ListNode pre1=pre;
		    ListNode p=cur;
	        for(int i=0;i<k;i++){
		        if(cur==null){
	             return dummy.next;
		        }
		        cur=cur.next;
		        pre1=pre1.next;
	        }
	        //能凑成一组，就翻转
	        ListNode groupStart=pre.next;
	        ListNode groupEnd=pre1;
	        ListNode nextStart=cur;
	        ListNode preGroup=null;
	        while(p!=null&&p!=nextStart){
		        ListNode temp=p.next;
		        p.next=preGroup;
		        preGroup=p;
		        p=temp;  
	        }
	       //preGroup这时是新组的第一个，进行连接,pre后面就是新顺序下的该组
	       pre.next=preGroup;
	       //原来的头是现在的组的尾接上下一组的头
	       groupStart.next=nextStart;
	       //移动pre到现在的尾,就是下一组的pre
	       pre=groupStart;
	        
        }
        
    }
}
class Solution {
    public ListNode reverseKGroup(ListNode head, int k) {
        ListNode dummy = new ListNode(-1);
        dummy.next = head;
        ListNode pre = dummy;
        ListNode cur = head;

        while (true) {
            // 1. 探测够不够 k 个（cur 走 k 步）
            ListNode probe = cur;                    // 探测用
            for (int i = 0; i < k; i++) {
                if (cur == null) return dummy.next;  // 不够 → 结束
                cur = cur.next;
            }
            // 走到这里说明够 k 个；probe 也可换成直接用 pre.next

            ListNode groupStart = pre.next;          // 当前组第一个
            ListNode nextStart = cur;                // 下一组第一个
            ListNode newHead = null;                 // 反转后的新头

            // 2. 反转这一组（206 三指针，到 nextStart 停）能凑成一组，就翻转
            ListNode p = groupStart;
            while (p != nextStart) {
                ListNode temp = p.next;
                p.next = newHead;
                newHead = p;
                p = temp;
            }

            // 3. 连接 + pre 接力
            // newHead这时是新组的第一个，进行连接,
            //pre后面就是新顺序下的该组
            pre.next = newHead;
            ////原来的头是现在的组的尾接上下一组的头
            groupStart.next = nextStart;
            //移动pre到现在的尾,就是下一组的pre
            pre = groupStart;
        }
    }
}

```

**20260809**

**Q2（ERR-017 坑1+坑4）**：判断对错——下面这段代码有两个 bug，指出来：

```java
public int get(int key) {
    if (map.containsKey(key)) {
        int res = map.get(key).value;
        moveToHead(map.get(key));
        //没有返回值，补充
        return res;
    }
    return -1;
}
```

java

以及

```java
private void removeTail() {
    DNode node = tail.prev;
    node.prev.next = tail;
    tail.prev = node.prev;
    //把原先节点摘除
    remove(node);
}
```

java

**Q3（REV-23 核心原理）**：LRU 为什么是"HashMap + 双向链表"？一个管什么，一个管什么？\
这个哈希map存储的是key和ke对应的节点。然后双向链表。它是就是用来存储这个。就是他最近使用最近使用的，他就把它拿到对头，最近最不实用的啊，反正就会在队尾，然后就是在需要移除的时候，把队尾的元素给节点移除。

20260810
 long pre=Long.MIN_VALUE;

    public boolean isValidBST(TreeNode root) {

        if(root==null) return true;

        //中序遍历

        //左边

        boolean left=isValidBST(root.left);

        //访问节点

        //判断该节点是否在区间内

        boolean flag= (root.val>pre);

        pre=root.val;

        boolean right=isValidBST(root.right);

        return flag&&left&&right;

    }
    ### Q2 · REV-18（递归骨架）→ 今天 7 题全用

递归三要素是"**基准 → 递归子问题 → 处理自己**"。用这个骨架，写求树最大深度的递归（104）：

```java
int maxDepth(TreeNode root) {
    if (_root==null) return __0_;           // ① 基准
    int left = _maxDepth(root.left);                  // ② 递归左
    int right = _maxDepth(root.right)__;                 // ② 递归右
    return (left>right?left:right)+1______;                      // ③ 处理自己（合并）
}
```

java

### Q3 · ERR-018（BFS 层序模板）→ 今天 199 右视图直接用

填空（考你昨天踩的 5 个坑）：

```java
Deque<TreeNode> queue = new ArrayDeque_();   // ① 用哪个类？或者LinkedList
queue.offer_(root);                      // ② 入队用哪个方法？
while (!queue.isEmpty()) {
    int size = queue._size_();           // ③ 先固定什么？
    for (int i = 0; i < size; i++) {
        TreeNode node = queue._poll_();  // ④ 取出并移除用哪个？
        if (node.left != null) queue.offer(node.left);
        if (node.right != null) queue.offer(node.right);
    }
}
```

java

### Q4 · REV-26（对称二叉树交叉比）→ 两棵树递归，今天 236 LCA 同款

对称二叉树 `check(p, q)` 的**两个递归调用**分别比的是哪两个节点？（填 p/q 的 left/right）

```java
return check(_p.left___, _q.right_) && check(_q.left___, _p.right_);
//       外侧比外侧        内侧比内侧
```

**20260811**
```java
class solution{
	TreeNode head=new ();
	public void flatten(TreeNode root) {

 
		
        if(root==null) return;
        
        flatten(root.right);
        flatten(root.left);
        root.left=null;
        root.right=head;
        head=root;
        
        

     

    }
}

```

```java
class solution{
	int max=
	public int maxPathSum(TreeNode root) {


    }
    //遍历每个节点，找每个节点的最大路径和
    int maxGain(TreeNode node){
    if(root==null) return 0;
    //找左边最大路径和
    int left=Math.max(maxGain(node.left),0);
    int right=Math.max(maxGain(node.right),0);
    //判断是不是最大值
    max=Math.max(left+right+node.val,max);
    //向上返回当前节点的单侧最大路径和，因为他这个向上传递的话只能选择一边
    return left>right?left+node.val:right+node.val;
    }
}

```
写出**头插法核心三行**（在链表里把新节点插到头部）：

```java
// 假设已有链表头 head，新节点 cur 要插到最前面：
newNode.next = head.next;
head.next = newNode;
return head.next;
```


```java
class Solution {
    public void flatten(TreeNode root) {
        TreeNode cur = root;
        while(cur!=null){
    
        //假如左子树存在，把左子树拿到右边
	        if(cur.left!=null){
		        TreeNode p=cur.left;
		        while(p.right!=null)p=p.right;//先找到左子树最右下节点
		        p.right=cur.right;//原右子树接入右下角
		        cur.right=cur.left;//左子树搬到右边
		        cur.left=null;//左边置空
	        }
	        cur=cur.right;//继续处理下一个，往下处理
        }
    }
}

```

20260812

##  热身一：REV-33 Java 基础小测（5 连问）

昨天 394 里你卡住的那些语法，今天验收。**每问都先自己想，别翻笔记**，直接写答案。

**Q1（StringBuilder）：**

```java
String s = "abc";
s += "d";        // 这个操作，s 的内容变了吗？，没有,这个能正常运行吗
StringBuilder sb = new StringBuilder("abc");
sb.append("d");  // 这个操作，sb 的内容变了吗？变成了abcd
```



👉 用一句话说说：**为什么写 394 这种"反复拼字符串"的场景，StringBuilder 比 String 快？**
不是太清楚，这个String不能直接在末尾添加，这个StringBuilder能在末尾直接添加的原因吗

**Q2（char→int）：**

```java
char c = '7';
int n = c - '0';   // n 等于几？7
```


👉 如果我不写 `- '0'`，直接 `int n = c;`，n 等于几？为什么？
就是字符c对应的ASCII码

**Q3（拼多位数）：**

```java
int num = 0;
for (char c : str.toCharArray()) {
    num = num * 10 + (c - '0');
}
```


👉 用这段逻辑手算一下：输入 `"354"`，num 的变化过程（写 3 步）。
最开始遍历到3，num是3，然后遍历到5，num变成35，遍历到4，num变成354（因为每次变化都相当于把原数字往前移了一位，符合这个十进制

**Q4（栈三连问）：**

```java
Deque<Integer> stack = new ArrayDeque<>();
stack.pop();   // 空栈 pop，会发生什么？会抛出错误
```


👉 所以弹出之前必须先写哪行判断？（这题你应该有肌肉记忆了）栈非空而且遇到了右括号

**Q5（数组默认值）：**

```java
int[] arr1 = new int[3];        // arr1[0] = ?默认值为0
Integer[] arr2 = new Integer[3]; // arr2[0] = ?默认值为null
```


👉 为什么一个默认是 0，另一个默认是 null？
前者是基本类型，后者是引用类型（但是为什么又说java里面没有真正意义上的引用类型呢
## ✍️ 自测：你来走一遍 `2[a3[c]]`

这张表我留了最后两列空，你**亲手填**（数字栈、字符串栈、当前工作区 res）：

| 读到  | 动作            | countStack | strStack    | res（当前工作区）       |
| --- | ------------- | ---------- | ----------- | ---------------- |
| `2` | 拼数字           | `[]`       | `[]`        | `""`             |
| `[` | 存档+清零         | `[2]`      | `[""]`      | `""`             |
| `a` | 字母            | `[2]`      | `[""]`      | "a"              |
| `3` | 拼数字           | `[2,3]`    | `[""]`      | "a"              |
| `[` | 存档+清零         | `[2,3]`    | `["", "a"]` | `""`             |
| `c` | 字母            | `[2,3]`    | `["", "a"]` | `"c"`            |
| `]` | k=3, prev="a" | `[2]`      | `[""]`      | `"accc"`（要写完整算式） |
| `]` | k=2, prev=""  | `[]`       | `[]`        | acccaccc（最终结果）   |

**填完检查：最终是不是 `"acccaccc"`？**

如果你能填对，说明你已经不是在背代码了，你是真的在"操作工作区"。填完发我，我们进入正题：**单调栈**。

20260813
**Q1（填空）：** 739 里我们写的是：

```java
while (!stack.isEmpty() && temperatures[stack.peek()] < cur)
```


为什么一定要写 `!stack.isEmpty()` 在前面？（提示：想想昨天那个 `push(0)` 之后弹空崩掉的例子）因为它如果当前这个站有一个元素，然后弹出去了，把它还在循环里面弹出去了，然后循环里面再次检验这个，就是这个栈顶查看，它就是可能会报出错误。嗯

**Q2（判断）：** 下面这段 84 单调栈的 left 边界代码，有几处不对？指出来：

```java
Deque<Integer> stack = new ArrayDeque<>();
for (int i = 0; i < n; i++) {
    while (!stack.isEmpty() && heights[stack.peek()] >= heights[i]) {
        stack.pop();
    }
    left[i] = stack.isEmpty() ? -1 : stack.peek();
    stack.push(i);
}
```



- 这是昨天给你的模板，应该没 bug。那你自己先讲一遍：**`>=` 为什么弹掉相等的？** 栈顶弹完之后为什么就是"左边第一个更矮的"？
- 、
- ## ✍️ 现在轮到你：写 right[]（对称版）

`right[i]` = **右边第一个更矮**的下标，没有则 `n`。**从右往左扫**，逻辑完全对称：

```java
// 第二遍：求 right[] = 右边第一个小于 heights[i] 的下标（没有则 n）
Deque<Integer> stack = new ArrayDeque<>();
for (int i = n - 1; i >= 0; i--) {        // 注意方向：从右往左
    while (!stack.isEmpty() && height[stack.peek()] >= heights[i]) {//这里是不是该不需要等于，如果有等于的话是不是就不会延伸，因为相等高度的也能构成矩形啊   // ① 弹谁？比照 left 想
        stack.pop();
    }
    right[i] = stack.isEmpty() ? _n : _stack.peek()_;           // ② 空栈返回 n，非空返回栈顶
    stack.push(i);
}
```

java

**填空前想两件事：**

1. 空①：left 里是 `heights[stack.peek()] >= heights[i]`，right 里**要不要改**？
2. 空②：左边界没有时返回 `-1`，那右边界没有时返回什么？（提示：右边界是"开区间"的右端，用来算宽度 `right - left - 1`）

**再加第三遍（面积）：**

```java
int res = 0;
for (int i = 0; i < n; i++) {
    res = Math.max(res, height[i] * (___right[i] - _left[i] - 1));   // ③ 高 × 宽
}
```

java

填完发我，我们一起验算是不是 10。你可以先用草稿本写，写出来我再帮你确认。

##  填空骨架：堆版

```java
public int findKthLargest(int[] nums, int k) {
    PriorityQueue<Integer> heap = new PriorityQueue<>();   // 小根堆
//优先队列就是天然的小根堆吗，对于优先队列的我的概念也不是很熟悉
    for (int num : nums) {
        heap.offer(num);                          // ① 先进堆
        if (heap.size()>k) {                                // ② 堆超过 k 个了
            heap.poll();                           // ③ 踢掉堆顶（最小的）
        }
    }
    return ___heap.peek();                                   // ④ 堆顶就是第 k 大
}
```

java

**填空前想：**

- 空②：什么时候该踢人？（堆大小 > k 还是 ≥ k？）
- 空④：返回 `heap.peek()` 还是 `heap.poll()`？（提示：返回完要不要删掉？）
## 🔴 第一批 · 三题默写

### 🧠 REV-29：437 路径总和 III（前缀和 + 回溯）

**回忆锚点**（只给这 4 个易错点，代码你自己写）：

1. **先查后记**：`res += map[currSum - target]` 必须在 `put` 之前（防 target=0 多算空路径）
2. **回溯撤销**：返回前 `map[currSum]--`（"借了要还"）
3. **初始化**：`map.put(0L, 1)`（让"从根开始"的路径可查）
4. **用 long**：前缀和可能超 int 范围

**题目：** 找出二叉树中**路径和等于 targetSum** 的路径数目（路径不必从根开始，也不必到叶子结束，但方向必须向下，即从某个节点一路到它的子孙节点）。

```
     10
    /  \
   5   -3
  / \    \
 3   2   11
  \   \
  3  -2
targetSum = 8  → 输出 3
（路径：5→3、5→2→-2、-3→11）
```

**请默写：** `pathSum(TreeNode root, int targetSum)` + 递归函数（Map<Long,Integer> 前缀和计数）。把 4 个易错点都用上。

### 📝 Q1：Arrays.fill（REV-53）

```java
char[][] board = new char[3][3];
// 用 Arrays.fill 把 board 的每一行都填成 '.'
for(char[] row:board){
Arrays.fill(row,'.');
}

// 应该怎么写？

```

java

（提示：`board` 是二维数组，`board[i]` 是每一行的一维数组。`Arrays.fill` 只能填一维的。）

---

### 📝 Q2：方法 `()` vs 数组 `[]`（REV-46 专项）

下面这些 Java 写法，哪个是**方法调用**（用 `()`），哪个是**数组取值**（用 `[]`）？判断对错：

```java
A. nums[i]        // 数组取值？对
B. s.charAt(0)    // 方法调用？对
C. map.get(k)     // 方法调用？对
D. s.charAt[0]    // 这样写对吗？错
E. list.get(i)    // 方法调用？对
```

java

把每个判断一下，告诉我哪个错。✍️

20260818
**Q1（ERR-024）** 这段撤销代码对吗？错在哪？

```java
path.deleteCharAt('(');//这个里面应该填对应的数字下标
```



**Q2（ERR-025）** 数组 `b` 长度 5，判断 `row` 越界，横线处填什么？

```java
if (row < 0 || row > b.length-1)
```


为什么不能用 `>`？row > b.length-1或者row >= b.length，因为length-1就是最后一个元素了

**Q3（REV-46 方法 vs 数组）** 下面哪个**编译报错**？

```java
s.charAt(i)                      // ①
nums[i]                          // ②
s.charAt[i]                      // ③错，方法调用应该小括号
map[digits.charAt(i) - '0']      // ④
```

20260819
**Q1（REV-46 方法 vs 数组）** 判断正误：`res.get(i-1).get(j-1)` 里，`get` 用的是圆括号——对吗？如果是数组，才用什么？对，用数字直接用方括号定位就行了

**Q2（REV-63 DP 滚动变量）** 70 爬楼梯迭代版，每轮循环结束前那两行"滚动更新"是什么？**198 打家劫舍漏写这两行会怎样？**
curCondition=lastCondition+llCondition
llCondition=lastCondition
lastCondition=curCondition
打家劫舍
//对于当前节点的两种选择方案，选择最大的作为其方案
dp[i]=Math.max(dp[i-1],dp[i-2]+nums[i])
**Q3（REV-61 45 跳跃游戏）** `jump` 里"什么时候 steps++"？一句话。这个好像记不得了，感觉突然又有点难理解了
```java
int maxPos=0;
int end=0;
int step=0;
for(int i=0;i<nums.length-1;i++){
	maxPos=Math.max(maxPos,i+nums[i]);
	if(i==end){
		step++;
		end=maxPos;
		if (maxPos >= nums.length - 1) break;
	}
}

```

**Q1 · REV-64 List 语法**

```java
List<Integer> row = new ArrayList<>();
row.add(3);
```

取第一个元素，下面哪个对？

```java
A. int x = row[0];
B. int x = row.get(0);//这个对，需要调用方法
```


**Q2 · REV-64 List 语法**

```java
List<Integer> row = new ArrayList<>(n);
```


这里面的 `n` 是干嘛的？（一句话）初始化ArrayList的容量

**Q3 · REV-67 零钱兑换**  
322 的 `dp` 为什么**必须用数组**，不能用 70/198 那样的滚动变量？一句话。
因为当前状态要由前面多个状态来确定，而不是固定的一两个

**Q4 · ERR-024 判断**

```java
path.deleteCharAt('(');
```
报错，这里应该填字符串对应的字符数组下标

这行代码会怎样？（不用纠结场景，直接说后果）
我觉得你可以总结一下动态规划这部分的题的解题方法，还有怎么判断一个题是什么背包，还有就是这些解法思路

```java
class Solution {
    public int longestValidParentheses(String s) {
        int max = 0;
        Deque<Integer> stack = new ArrayDeque<>();
        stack.push(-1);   // ⭐ 哨兵：表示"最后一个匹配失败的位置"。初始 -1，让从 0 开始的有效串也能算长度

        for (int i = 0; i < s.length(); i++) {
            if (s.charAt(i) == '(') {
                stack.push(i);          // 左括号：入栈，等配对（存下标 i）
            } else {                    // 右括号：尝试配对
                stack.pop();            // 先弹栈顶 —— 可能是"配对的左括号"，也可能是"哨兵"
                if (stack.isEmpty()) {
                    stack.push(i);      // 栈空了 → 这个右括号没人配，是"新的哨兵"，压入
                } else {
                    // 栈顶现在是"最后一个未匹配位置"，中间这段全是有效括号
                    max = Math.max(max, i - stack.peek());
                }
            }
        }
        return max;
    }
}
```

**Q1 · REV-70（300 最长递增子序列）**  
`dp[i]` 代表什么？为什么转移时要**枚举所有 j**，而不是只看 i-1？
dp[i]代表当前位置的最长递增子序列长度,因为有可能，i-1的状态只能在i-1构成递增序列，
比如5,6,3在这里i-1构成递增序列，但是i不构成，还有，1,10，5,11，12,13，9，14,这个14的状态基本上和前面9所构成的递增序列不一样

**Q2 · REV-71（152 乘积最大子数组）**  
为什么这题要**同时维护 curMax 和 curMin** 两个变量？只记一个行不行？
不行，因为值存在为负数的风险，负数乘负数又变成正数

**Q3 · REV-72 + REV-67（背包顺序对照）**

- 416 分割等和子集，内层循环为什么**倒序**？
- 因为dp里面，每个和的值只用一次，感觉我还是叙述不清楚，大概距离，dp：1,2；数组：【1】,如果从小到大dp[1]=dp[],感觉我也说不好，还是理解不到位
> "正序找 `dp[j-nums[i]]` 的时候就**可能会重复使用之前的小 dp 的结论**——`j_大 - nums[i] = j_old`，而 `j_old` 已经拿过一个 `nums[i]` 了"

> （临时数组版）"该数字只能放入求和子数组一次，所以每次**要先用旧的 dp**"
- 322 零钱兑换，内层循环为什么**正序**？  
    （一句话说清区别）
    这个不限制数量正序倒序都无所谓吧，是不是

**Q4 · REV-73（32 最长有效括号）**  
栈里存的是**下标**还是**括号字符**？为什么？
是下标，因为要算长度

20260822
## 🔔 开场快问（4 题 · 思路检验）

**Q1 · REV-74（62 不同路径）**  
`dp[i][j]` 代表什么？状态转移是什么？（一句话）
代表当前位置走到终点有几条路
dp[i][j]=dp[i+1][j]+dp[i][j+1](前提是i+1和j+1在地图范围内)

**Q2 · REV-75（64 最小路径和）**  
62 和 64 是同一个骨架（矩阵路径），唯一区别是什么？
都是找终点，一个是找出到终点的路径数，一个是找到终点的最小路径和（这个就要考虑每个位置的数值了）

**Q3 · REV-76（二维数组初始化）⭐ 你点名要复习的**  
要初始化一个二维 `int[][] memo` 全部为 -1，下面哪种写法对？

```java
A. Arrays.fill(memo, -1);
B. for (int[] row : memo) Arrays.fill(row, -1);
```


为什么？
B，因为Arrays.fill()只能填充一维数组

**Q4 · REV-77（5 最长回文子串）**  
中心扩展法：遍历的"中心"一共有几个？（提示：奇回文 + 偶回文）统一写法的 `l` 和 `r` 怎么算？
两种遍历，一种是回文串为奇数，找一个字符往两边遍历，不是回文串就切换下一个中心
一个是偶数，这个就要同时将l，r同时初始化在两个相邻位置
统一写法l=i/2,r=(i+1)/2(因为i是从0开始)

## 🔔 开场快问（4 题）

**Q1 · REV-78（1143 最长公共子序列）**  
`dp[i][j]` 表示"text1 前 i 个 vs text2 前 j 个"。两个转移分支各是什么？为什么"不等"用 `max` 
dp[i][j]=dp[i-1][j-1]+1(条件，当前字符s[i-1]（第i个字符）与字符他【j-1】相等)
dp[i][j]=max(dp[i][j-1]，dp[i-1][j])(当前指针所指的字符不相等，那么就将当前两个字符串f分别拆出去一个（可能这种情况存在更长的子串)，感觉这里我理解还是有问题，不太懂为什么每边要单独只拆一个

**Q2 · REV-79（72 编辑距离）**  
字符不等时，`min` 里三个方向 `dp[i-1][j]`、`dp[i][j-1]`、`dp[i-1][j-1]` 分别对应什么操作？删除（删除当前字符，看看前一个字符与当前指针是否匹配来找这个匹配组合的最少操作，相当于就是往前移指针，这里加一次操作）
	插入（在当前匹配串加一个相同的使得位置能够匹配（dp[i][j-1]就是将模式串的指针向前移，因为这个插入操作已经使得j位的字符匹配了），这里加一次操作
	替换（原位置换一个合适的数，当前两指针匹配，然后指针继续同时向前移动，操作次数加1


**Q3 · REV-76（二维数组初始化）**  
把二维 `int[][] memo` 全部填成 -1，代码怎么写？（两行）
for(int[]me:memo)Arrays.fill(me,-1);

**Q4 · REV-77（5 最长回文子串·中心扩展）**  
统一写法 `l` 和 `r` 怎么算？一共遍历几个中心？（一句话）
l=i/2,r=(i+1)/2,i使当前下标，i为奇数，当前字符串个数就为偶数，
一共要遍历长度个数？