# CLAUDE.md · 项目规则（AI 专用）

> 本文件是 AI 在本仓库工作的行为规范，每次对话开始时应隐式遵守。

---

## 一、项目简介

- **类型：** Obsidian Vault（LeetCode Hot 100 学习体系，Java 版）
- **结构：** 见 `00-配置/全局索引.md`
- **AI 操作手册：** `学习中枢.md`（每次对话前必读）

---

## 二、Git 自动上传规则（核心 ⭐）

### 2.1 触发时机

**自动触发（默认）：** 每次 AI 对 vault 内文件做出**修改**后（新增/编辑/删除），**必须**自动执行一次 git 提交并推送。

**手动触发：** 用户说"更新""上传""推一下"等指令时，立即执行。

### 2.2 执行流程

每次修改完成后，按顺序执行：

```bash
# 1. 查看改动（确认提交范围）
git status

# 2. 添加所有改动（.gitignore 已过滤本地配置）
git add -A

# 3. 提交（提交信息规范见 2.3）
git commit -m "<type>: <描述>"

# 4. 推送到 GitHub
git push
```

### 2.3 提交信息规范

| 前缀 | 用途 | 示例 |
|------|------|------|
| `docs:` | 文档/笔记修改 | `docs: 完成 Day1 前三题学习记录` |
| `feat:` | 新增文件/功能 | `feat: 新增纠错文档 05-纠错文档` |
| `fix:` | 修正错误 | `fix: 修正 03 教程文件语法笔误` |
| `chore:` | 配置/杂项 | `chore: 更新 .gitignore` |
| `test:` | 测验/自测 | `test: Day1 随堂测验批改` |

**规则：** 简洁中文描述，一句话说清改了什么。多个改动合并时，取最重要的前缀。

### 2.4 例外情况（不自动提交）

| 情况 | 处理 |
|------|------|
| 用户明确说"先别传" | 只做修改，不提交 |
| 提交失败（网络/冲突） | 告知用户，不强制 |
| 用户自己的临时改动未完成 | 不擅自提交用户未完成的改动 |

### 2.5 提交安全

- `.gitignore` 已忽略 `.obsidian/`、`.claudian/`（本地配置与 AI 会话，不上传）
- **绝不**提交涉及个人隐私的文件（凭据、token、密钥等）
- 遇到疑问时先问用户，不擅自删除或覆盖

---

## 三、文件路径约定

- Vault 内引用一律用 `[[wikilink]]` 格式
- 相对路径操作，禁止使用绝对路径访问 vault 文件

---

## 四、维护

- 本文件 + `学习中枢.md` 是双规则源，两者保持一致
- 修改本文件后，同步更新 `学习中枢.md` 的变更日志
